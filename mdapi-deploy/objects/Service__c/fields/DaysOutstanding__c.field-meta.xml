<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>DaysOutstanding__c</fullName>
    <description>Number of days since service was rendered for records with an outstanding balance. Returns null if balance is zero or service not yet rendered.</description>
    <externalId>false</externalId>
    <formula>IF(
    AND(
        BalanceDue__c &gt; 0,
        NOT(ISBLANK(ServiceRenderedDate__c))
    ),
    TODAY() - DATEVALUE(ServiceRenderedDate__c),
    null
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Days Outstanding</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
